#include <stdio.h>
/**
 * main - this function to start coding
 *
 * Description: print some lower case alphabet
 *
 * putchar: to print the target
 *
 * Return: 0 Always
 */
int main(void)
{
	char l;
i
	for (l = 'a'; l <= 'z'; l++)
	{
		putchar(l);
	}
p:utchar('\n');

return (0);
}
