#include "main.h"
/**
 * mul - function to multiply 2 numbers
 *
 * @a: parameter to identify numbers
 *
 * @b: prameter to identify numbers
 *
 * Return: x always
 */

int mul(int a, int b)
{
	int sum;

	sum = a * b;
return (x);
}
