#include "main.h"
#include <stdlib.h>
/**
 * main - Entry point
 *
 * _putchar: print the target
 *
 * Description: printing absolute value of numbers
 *
 * Return: always 0
 */

int _abs(int);
{
	if ( n > 0)
		return(-n);
	else
		return (n);
}

