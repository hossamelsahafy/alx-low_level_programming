#include "main.h"

/**
 * set_string - function to set the value of a pointer to a char
 *
 * @s: parameter that point to char
 *
 * @to: parameter that poin to char
 */
void set_string(char **s, char *to)
{
	*s = to;
}
